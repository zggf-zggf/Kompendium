#include <bits/stdc++.h>

using namespace std;

int main(){
	vector<int> tab;
	for(int i = 0; i < 10; i++){
		int a; cin>>a;
		tab.push_back(a);
	}
	sort(tab.begin(), tab.end());
	for(int i = 1; i < tab[0]; i++){
		
	}
	return 0;
}
